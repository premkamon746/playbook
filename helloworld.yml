---
- name: Hello World!
  hosts: all
 
  tasks:
 
  - name: Hello World!
    shell: echo "Hi! Tower is working. "
    register: hello

  - name: Debug hello
    - "{{ hello.stdout }}"

  - name: Debug hello
    - "{{ hello.stderr }}"
